/**
 * Union of built-in HTML element types that expose the `form` property.
 */
type FormAssociatedElement =
    | HTMLInputElement
    | HTMLTextAreaElement
    | HTMLSelectElement
    | HTMLButtonElement
    | HTMLFieldSetElement
    | HTMLObjectElement
    | HTMLOutputElement;
